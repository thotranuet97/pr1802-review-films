default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  host: localhost
  database: review-film
  username: root
  password:
development:
  <<: *default

test:
  <<: *default
  database: <%= ENV["DATABASE_NAME_TEST"] %>

staging:
  <<: *default

production:
<<: *default